# RM Shared
Общие типы, константы, конфиги комнат, JSON-схемы.  
Пока заготовка.

## Events & Schemas
Файлы `events.ts` и `schemas/*.json` содержат формальные контракты между клиентом и сервером RM.
Они гарантируют, что физика и экономика боёв синхронизированы строго по JSON-схемам.
